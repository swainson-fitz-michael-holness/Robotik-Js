function alpha_sort(str) {
var arr = [];
var update = str.replace(/\W/g, " ");
arr = update.split(" ");
for(var i = 0; i < arr.length; i++) {
  if(arr[i] === "") {
    arr.splice(i,1);
  }
}
arr = arr.sort();
return arr;
}
